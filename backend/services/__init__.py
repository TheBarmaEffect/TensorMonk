"""Services layer — export pipeline, graph visualization, and session management.

Modules:
    export_service: Generates Markdown, PDF, DOCX, and JSON reports
    graph_visualizer: Produces pipeline topology for frontend visualization
    session_manager: Session state machine with FSM lifecycle tracking
"""
